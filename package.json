{
  "name": "turbo-signal",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "Markus Walther",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "bun serve.js",
    "build": "bun run terser src/index.js --module --compress ecma=2022,passes=2 --mangle > dist/index.min.js && bun run compress.js",
    "format": "prettier -w src/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "brotli": "^1.3.3",
    "prettier": "^3.5.3",
    "rollup": "^4.41.1",
    "terser": "^5.41.0"
  }
}
